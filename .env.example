# Path to the catalog the backend opens read-only.
# Default: data/books.db
GANZACH_DB=data/books.db

# Set to 0 to disable /api/read (the PDF streaming proxy).
# With it off, a framing block leaves only the "open in a new tab" fallback.
GANZACH_PROXY=1
